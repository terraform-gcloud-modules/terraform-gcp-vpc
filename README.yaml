---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name : Terraform GCP Module Template

# License of this project
license: "APACHE"

# Canonical GitHub repo
github_repo: terraform-gcloud-modules/terraform-gcp-vpc

# Badges to display
badges:
  - name: "Terraform"
    image: "https://img.shields.io/badge/Terraform-v0.13-green"
    url: "https://www.terraform.io"
  - name: "Licence"
    image: "https://img.shields.io/badge/License-APACHE-blue.svg"
    url: "LICENSE.md"
  - name: "Changelog"
    image: "https://img.shields.io/badge/Changelog-blue"
    url: "CHANGELOG.md"

# Prerequisites to display
prerequesties:
  - name: Terraform
    url: https://learn.hashicorp.com/terraform/getting-started/install.html
    version: ">= 1.12"

providers:
  - name: google
    url: https://cloud.google.com/
    version: ">= 4.64, < 8"

# description of this project
description: |-
  Terraform GCP Module Template to create new modules using this as baseline

# How to use this project
usage: |-
  Here are some examples of how you can use this module in your inventory structure:
  ```hcl
  module "vpc" {
  source = "../.."

  # Module control
  module_enabled                    = true
  google_compute_network_enabled    = true
  enable_private_ip_alloc           = true
  enable_service_networking         = true

  # General settings
  name        = local.name
  description = "VPC network for dev and testing workloads"
  project_id  = "project-id"
  environment = local.environment
  label_order = local.label_order

  # Network configuration
  auto_create_subnetworks         = false
  routing_mode                    = "GLOBAL"
  mtu                             = 1460
  delete_default_routes_on_create = false
  enable_ula_internal_ipv6        = false
  internal_ipv6_range             = null
  }
  ```
